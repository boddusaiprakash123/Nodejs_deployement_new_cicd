version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Installing source node dependencies...
      - cd /root/Nodejs_deployment-new_cicd/node_deployment
      - npm i --force
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the node code
  post_build:
    commands:
      - echo Build completed on `date`
